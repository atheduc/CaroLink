import React from 'react';
import './Background.css'; // Ensure you have this CSS file for styling

const Background = () => {
  return (
    <div className="background">
      {/* The background is white, and we’ll handle text inside the Welcome page */}
    </div>
  );
};

export default Background;
